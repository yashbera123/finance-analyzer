# Manual Review Data

This folder is for preserved local data that should not be committed or deployed automatically.

Current preserved item:

- `outer_finance_analyzer.db` - SQLite database found in the original archive root, outside the application directory.
